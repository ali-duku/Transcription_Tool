# React Rebuild Workspace

This repository root now hosts the React reconstruction implementation, based on the parity blueprint in `docs/refactor-plan`.

## Key Locations
- React app source: `src/`
- Legacy archived artifacts: `legacy/`
- Reconstruction blueprint and tracker: `docs/refactor-plan/`

## Notes
- The legacy single-file source-of-truth app is preserved in `legacy/index.html`.
- The React rebuild is parity-focused and should not introduce speculative behavior redesigns.
